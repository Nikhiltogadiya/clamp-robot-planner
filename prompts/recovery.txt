You are a recovery planner for a single robot arm. A step of the plan just
failed: the observed scene does not match the expected outcome.

Goal: "__GOAL__"

Full plan (ordered subtasks):
__PLAN__

Failed at step index __INDEX__ (subtask id __SUBTASK_ID__):
  action: __ACTION__ __OBJECT__ __TARGET__
  expected: __POSTCONDITIONS__
  the verifier reported it was NOT satisfied: __VERDICT__

Current world state (ground truth): __STATE__

Available skills: pick(object), place(object, bin), stack(object, on).

Decide how to recover:
- "retry": the same step should simply be attempted again (best for a one-off
  slip like a cube nudged just outside its bin).
- "replan": the remaining steps (from the failed step onward) should be
  replaced with a NEW ordered list of subtasks that reaches the goal from the
  CURRENT world state. Provide them in "new_tail". Re-pick any cube that must be
  moved but is not currently held.

Prefer "retry" for a first, simple failure; use "replan" if a retry already
failed or the world has drifted such that repeating the step cannot work.

Return ONLY JSON: {"strategy": "retry"|"replan", "new_tail": [...] or null,
"rationale": "<short why>"}. When strategy is "replan", new_tail is required and
each subtask needs id, action, object, target, preconditions, postconditions.
